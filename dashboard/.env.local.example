# Copy to .env.local and set the IP of your Raspberry Pi on your network.
# When dev'ing on the Pi itself: NEXT_PUBLIC_API_URL=http://localhost:8000
# When dev'ing from another machine on the same WiFi: use the Pi's LAN IP.
NEXT_PUBLIC_API_URL=http://localhost:8000
